import React from 'react';

export default function Brightcove() {
  return (
    <div className="brightcove">
    </div>
  );
}

if (process.env.NODE_ENV !== 'production') {
  Brightcove.propTypes = {
  };
}
